# Python
.venv/
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/

# Environment
.env
*.env

# macOS
.DS_Store

# Editor
.idea/
.vscode/

# Runtime artifacts (job workspaces generated by the pipeline — not source)
memory/job_workspaces/

# Benchmark reports (generated output — not source)
reports/

# Claude Code worktrees (session-local, not source)
.claude/worktrees/

# Duplicate directory (macOS artifact from directory name with spaces)
Master Gaming App and OS/

# Debug payloads written by shared_agent_runner on validation failure
memory/**/*.INVALID_DEBUG.*.json

# Ephemeral job snapshot outputs (generated per run, not source)
artifacts/jobs/

# Library backups (safety net from write-back applies, not source)
artifacts/misconception_library/backups/
